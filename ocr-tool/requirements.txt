# Core deps cho dự án tiêu thụ (Modal-backed, không cần GPU stack)
modal>=1.5
aiohttp
PyYAML
PyMuPDF
Pillow
fsspec
wordfreq

# OPTIONAL: chỉ cần nếu chạy engine OCR tại chỗ trên máy có GPU
# (không cần cho đường Modal — GPU stack đã nướng sẵn trong image trên Modal)
# torch
# vllm
# onnxruntime-gpu
# transformers
# paddleocr  # layout model PP-DocLayoutV3

# OPTIONAL: chỉ cho nhánh scan-pdf text detection (cần binary tesseract trên hệ thống)
# pytesseract
